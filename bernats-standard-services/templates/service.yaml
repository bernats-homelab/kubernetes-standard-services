apiVersion: v1
kind: Service
metadata:
  name: {{ include "bernats-standard-services.fullname" . }}
  labels:
    {{- include "bernats-standard-services.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bernats-standard-services.selectorLabels" . | nindent 4 }}
